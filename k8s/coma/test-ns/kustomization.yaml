apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./namespace.yaml
  - ./test-resource.yaml
spec:
  postBuild:
    substituteFrom:
      - kind: Secret
        name: test-secret
  dependsOn:
    - name: overlay
