apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-pvc
  namespace: navidrome
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: media-pv
