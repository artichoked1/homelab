apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: soulbeet-secret-key
  namespace: media
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: soulbeet-secret-key
  data:
    - secretKey: secret-key
      remoteRef:
        key: soulbeet/secret-key
        property: secret-key
