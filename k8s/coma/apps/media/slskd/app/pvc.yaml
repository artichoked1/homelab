apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: slskd-media-pvc
  namespace: media
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: slskd-media-pv
