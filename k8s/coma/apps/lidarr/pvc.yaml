apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lidarr-media-pvc
  namespace: lidarr
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: lidarr-media-pv
