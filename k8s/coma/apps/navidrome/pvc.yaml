apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: navidrome-media-pvc
  namespace: navidrome
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: navidrome-media-pv
