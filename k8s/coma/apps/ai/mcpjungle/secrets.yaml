apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mcpjungle-postgres-auth
  namespace: ai
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: mcpjungle-postgres-auth
  data:
    - secretKey: username
      remoteRef:
        key: mcpjungle/postgres-auth
        property: username
    - secretKey: password
      remoteRef:
        key: mcpjungle/postgres-auth
        property: password