apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: koel-media-pvc
  namespace: koel
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: koel-media-pv
