apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: soularr-downloads-pvc
  namespace: soularr
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: soularr-downloads-pv
