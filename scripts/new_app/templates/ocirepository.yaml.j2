apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: OCIRepository
metadata:
  name: {{ metadata_name }}
  namespace: flux-system
spec:
  interval: {{ interval }}
  url: "{{ url }}"
  ref:
    tag: "{{ tag }}"
